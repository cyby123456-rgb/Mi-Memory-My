services:
  mimemory:
    build: .
    ports:
      - "8765:8765"
    environment:
      RETENTION_DAYS: "30"
      MIMEMORY_API_TOKEN: "${MIMEMORY_API_TOKEN:-}"
    env_file:
      - .env
    volumes:
      - mimemory-data:/data
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8765/health', timeout=2)"]
      interval: 10s
      timeout: 3s
      retries: 5
    restart: unless-stopped

volumes:
  mimemory-data:
